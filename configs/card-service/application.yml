# Card Service 설정

server:
  port: ${SERVER_PORT:8084}

spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/card_db}
    username: ${DB_USERNAME:card}
    password: ${DB_PASSWORD:card}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      idle-timeout: 300000
      connection-timeout: 20000
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
  kafka:
    consumer:
      group-id: card-service

# Logging
logging:
  level:
    com.junbank.cardservice: DEBUG
    org.hibernate.SQL: DEBUG